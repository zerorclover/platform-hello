{
  "selected_environment": "dev",
  "jobs": {
    "deploy": {
      "environment": "${{ inputs.environment }}",
      "needs": ["package-images", "secret-scan", "security-scan", "terraform-validate", "policy-environment"]
    }
  }
}
